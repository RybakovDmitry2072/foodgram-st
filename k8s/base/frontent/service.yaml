apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: foodgram
spec:
  type: ClusterIP
  selector:
    app: foodgram-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80